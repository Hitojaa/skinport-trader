# ============================================
# CONFIGURATION SKINPORT TRADER
# Copie ce fichier en .env et remplis tes valeurs
# ============================================

# === SKINPORT API (REQUIS) ===
# Obtenir sur: https://skinport.com/settings
SKINPORT_CLIENT_ID=ton_client_id_ici
SKINPORT_CLIENT_SECRET=ton_client_secret_ici

# === BASE DE DONNÉES ===
# SQLite (par défaut, pour local)
DATABASE_URL=sqlite:///data/skinport_trading.db

# PostgreSQL (pour production)
# DATABASE_URL=postgresql://user:password@localhost:5432/skinport_trading

# === PARAMÈTRES DE TRADING ===
# Seuil z-score pour sous-évaluation (-2.2 = 2.2 écarts-types sous moyenne)
Z_SCORE_THRESHOLD=-2.2

# Volume minimum de ventes sur 24h
MIN_VOLUME_24H=5

# Edge minimum net après frais (%)
MIN_EDGE_NET=3.0

# Prix maximum d'un item (€)
MAX_ITEM_PRICE=100.0

# Position maximale totale (€)
MAX_POSITION_SIZE=200.0

# === ALERTES DISCORD (RECOMMANDÉ) ===
# Créer un webhook: Paramètres serveur > Intégrations > Webhooks
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...

# === ALERTES TELEGRAM (OPTIONNEL) ===
# Créer un bot: @BotFather sur Telegram
# TELEGRAM_BOT_TOKEN=123456:ABC-DEF...
# TELEGRAM_CHAT_ID=123456789

# === COLLECTEUR ===
# Nombre d'items analysés par scan
MAX_ITEMS_PER_SCAN=10

# Délai entre requêtes API (secondes) - NE PAS DESCENDRE SOUS 45s !
RATE_LIMIT_DELAY=45.0

# Intervalle entre scans complets (minutes)
SCAN_INTERVAL=60
